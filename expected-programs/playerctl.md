# playerctl

`playerctl` is a command-line utility and library for controlling media players supporting the MPRIS D-Bus interface.

## Installation

*   **Debian / gLinux (Work)**:
    ```bash
    sudo apt install playerctl
    ```
*   **Fedora (Personal)**:
    ```bash
    sudo dnf install playerctl
    ```

# doom

`doom` (Doom Emacs) is an Emacs framework for hackers and keyboard enthusiasts.

## Installation

*   **Git Clone / Setup**:
    ```bash
    git clone --depth 1 https://github.com/doomemacs/doomemacs ~/.config/emacs
    ~/.config/emacs/bin/doom install
    ```

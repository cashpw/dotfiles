# unzip

`unzip` is a utility for extracting compressed ZIP archives.

## Installation

*   **Debian / gLinux (Work)**:
    ```bash
    sudo apt install unzip
    ```
*   **Fedora (Personal)**:
    ```bash
    sudo dnf install unzip
    ```

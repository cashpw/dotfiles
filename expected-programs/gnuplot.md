# gnuplot

`gnuplot` is a command-line driven graphing utility used in Emacs for generating plots from Org mode tables and code blocks.

## Installation

*   **Debian / gLinux (Work)**:
    ```bash
    sudo apt install gnuplot
    ```
*   **Fedora (Personal)**:
    ```bash
    sudo dnf install gnuplot
    ```

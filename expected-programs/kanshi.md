# kanshi

`kanshi` allows dynamic display configuration for Wayland compositors (like Sway), switching monitor profiles when displays are plugged or unplugged.

## Installation

*   **Debian / gLinux (Work)**:
    ```bash
    sudo apt install kanshi
    ```
*   **Fedora (Personal)**:
    ```bash
    sudo dnf install kanshi
    ```

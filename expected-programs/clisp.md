# clisp

`clisp` is an implementation of ANSI Common Lisp, used in Emacs helper functions for number formatting.

## Installation

*   **Debian / gLinux (Work)**:
    ```bash
    sudo apt install clisp
    ```
*   **Fedora (Personal)**:
    ```bash
    sudo dnf install clisp
    ```

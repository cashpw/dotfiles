# emacs

`emacs` is an extensible, customizable, free/libre text editor and computing environment.

## Installation

*   **Debian / gLinux (Work)**:
    ```bash
    sudo apt install emacs
    ```
*   **Fedora (Personal)**:
    ```bash
    sudo dnf install emacs
    ```

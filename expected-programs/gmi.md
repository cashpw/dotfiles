# gmi (lieer)

`gmi` (Lieer) is a fast 2-way Gmail sync tool for Notmuch, used in Emacs for sending and syncing email.

## Installation

*   **Debian / gLinux (Work)**:
    ```bash
    sudo apt install lieer
    ```
*   **Fedora / Pip (Personal)**:
    ```bash
    pip install lieer
    ```

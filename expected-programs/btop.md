# btop

`btop` is a terminal resource monitor showing usage and stats for processor, memory, disks, network, and processes.

## Installation

*   **Debian / gLinux (Work)**:
    ```bash
    sudo apt install btop
    ```
*   **Fedora (Personal)**:
    ```bash
    sudo dnf install btop
    ```

# font-manager

`font-manager` is a font management application for GTK Desktop Environments.

## Installation

*   **Debian / gLinux (Work)**:
    ```bash
    sudo apt install font-manager
    ```
*   **Fedora (Personal)**:
    ```bash
    sudo dnf install font-manager
    ```

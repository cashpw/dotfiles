# ncspot

`ncspot` is an ncurses Spotify client written in Rust.

## Installation

*   **Debian / gLinux (Work)**:
    ```bash
    cargo install ncspot
    ```
*   **Fedora (Personal)**:
    ```bash
    sudo dnf install ncspot
    ```

# fcitx5

`fcitx5` is a flexible multilingual input method framework for Linux and Wayland.

## Installation

*   **Debian / gLinux (Work)**:
    ```bash
    sudo apt install fcitx5
    ```
*   **Fedora (Personal)**:
    ```bash
    sudo dnf install fcitx5
    ```

# nmtui

`nmtui` is a text user interface utility for controlling NetworkManager and managing Wi-Fi and network connections.

## Installation

*   **Debian / gLinux (Work)**:
    ```bash
    sudo apt install network-manager
    ```
*   **Fedora (Personal)**:
    ```bash
    sudo dnf install NetworkManager-tui
    ```

# kitty

`kitty` is a GPU-accelerated terminal emulator, used as the primary terminal emulator in Sway.

## Installation

*   **Debian / gLinux (Work)**:
    ```bash
    sudo apt install kitty
    ```
*   **Fedora (Personal)**:
    ```bash
    sudo dnf install kitty
    ```

# libreoffice

`libreoffice` is an office suite used by Org mode for opening documents (`.doc`, `.xls`, `.ppt`).

## Installation

*   **Debian / gLinux (Work)**:
    ```bash
    sudo apt install libreoffice
    ```
*   **Fedora (Personal)**:
    ```bash
    sudo dnf install libreoffice
    ```

# pandoc

`pandoc` is a universal document converter used by Emacs for converting Markdown, Org, HTML, and other formats.

## Installation

*   **Debian / gLinux (Work)**:
    ```bash
    sudo apt install pandoc
    ```
*   **Fedora (Personal)**:
    ```bash
    sudo dnf install pandoc
    ```

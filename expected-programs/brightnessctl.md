# brightnessctl

`brightnessctl` is a utility for controlling device display brightness.

## Installation

*   **Debian / gLinux (Work)**:
    ```bash
    sudo apt install brightnessctl
    ```
*   **Fedora (Personal)**:
    ```bash
    sudo dnf install brightnessctl
    ```

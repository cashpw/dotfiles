# voxtype

`voxtype` is a fast local voice dictation tool for Linux/Wayland powered by Whisper.

## Installation

*   **Dotfiles Installation Script**:
    Run the repo script to install the binary to `~/.local/bin/voxtype` and download Whisper models:
    ```bash
    bash scripts/config/install_voxtype.sh
    ```

# grim

`grim` is a command-line screenshot utility for Wayland compositors.

## Installation

*   **Debian / gLinux (Work)**:
    ```bash
    sudo apt install grim
    ```
*   **Fedora (Personal)**:
    ```bash
    sudo dnf install grim
    ```

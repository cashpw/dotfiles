# fzf

`fzf` is a general-purpose command-line fuzzy finder.

## Installation

*   **Debian / gLinux (Work)**:
    ```bash
    sudo apt install fzf
    ```
*   **Fedora (Personal)**:
    ```bash
    sudo dnf install fzf
    ```

# dunst

`dunst` is a lightweight notification daemon.

## Installation

*   **Debian / gLinux (Work)**:
    ```bash
    sudo apt install dunst
    ```
*   **Fedora (Personal)**:
    ```bash
    sudo dnf install dunst
    ```

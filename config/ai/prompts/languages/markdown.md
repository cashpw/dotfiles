<markdown_style>

Ensure all generated Markdown content complies with the following precise formatting rules:

1.  **List Spacing**:
    - You MUST place an empty line (newline) both above and below the entire list.
    - Do NOT add empty lines between items within a list, nor above/below nested sub-lists.
2.  **Link Formatting**:
    - You MUST write links using standard syntax: `[Link Text](URL)`.
    - When using backticks in links, you MUST place the backticks inside the square brackets (e.g., `[`file.py`](file://...)`).
    - Never place backticks outside the square brackets (e.g., do NOT write `` `[file.py](file://...)` ``), as this breaks link rendering.

</markdown_style>
